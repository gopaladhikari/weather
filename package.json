{
  "dependencies": {
    "express": "^4.18.2",
    "hbs": "^4.2.0"
  },
  "name": "web",
  "version": "1.0.0",
  "main": "./src/app.js",
  "scripts": {
    "server": "NODE_ENV=development ./node_modules/.bin/.netlify-lamba serve src",
    "build": "NODE_ENV=production ./node_modules/.bin/.netlify-lamba serve src",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "description": ""
}
